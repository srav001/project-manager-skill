interface:
  display_name: "Project Manager Role"
  short_description: "Isolated project delivery for ADHD users"
  default_prompt: "Use $project-manager-role as my ADHD-friendly Project Manager to isolate this feature in a temporary Git worktree, coordinate phased implementation and dual review, verify it through a production-like preview on free ports, then ask before transferring the exact diff into the clean source checkout and ask again before committing and pushing."
